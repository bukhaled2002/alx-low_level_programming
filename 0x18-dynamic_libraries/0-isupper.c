#include "main.h"

/**
 * _isupper - sadds akfasfas madsksa
 * @c: sjsdsajskldsadas
 * Return: 1 Success 0 else
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
