#include "main.h"
#include <unistd.h>
/**
 * _islower - any description
 * @c: balssalsas adsa
 * Return: 1 (Success)
 */
int _islower(int c)
{
if (c >= 'a' || c <= 'z')
	return (1);
else
	return (0);
}
