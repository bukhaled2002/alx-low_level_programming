#include "main.h"
/**
 * _isupper - sadds akfasfas madsksa
 * @c: sjsdsajskldsadas
 * Return: 1 Success
 */
int _isupper(int c)
{
	char ch;
	int v;

	ch = c + '0';
	if (ch >= 'A' && ch <= 'Z')
		v = 1;
	else
		v = 0;
return (v);

}
