#include "main.h"

/**
 * _isdigit - sadds akfasfas madsksa
 * @c: sjsdsajskldsadas
 * Return: 1 Success 0 else
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
